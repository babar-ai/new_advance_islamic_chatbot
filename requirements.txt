# ── LangChain Core Stack ──────────────────────────────────────────────
langchain-community          # DirectoryLoader, TextLoader, and other loaders
langchain-core               # Document, BaseLoader base classes
langchain-text-splitters     # RecursiveCharacterTextSplitter for chunking

# ── Vector Database ───────────────────────────────────────────────────
langchain-qdrant             # LangChain ↔ Qdrant integration
qdrant-client                # Direct Qdrant Python client (REST + gRPC)
fastembed                    # Qdrant BM25 sparse embeddings (offline, no API cost)

# ── OpenAI & Environment ──────────────────────────────────────────────
langchain-openai
python-dotenv

pydantic_settings
langgraph
tavily
redis

# ── Observability & Evaluation ────────────────────────────────────────
langsmith                    # LangSmith tracing + feedback client
ragas==0.2.15              # RAG evaluation metrics — pinned for langchain-community compatibility
datasets                     # HuggingFace datasets — required by ragas

langgraph-cli[inmem]

fastapi-limiter==0.1.6       # Pinned — v0.2 broke the FastAPILimiter.init() API
fastapi
uvicorn[standard]
httpx
cachetools
numpy